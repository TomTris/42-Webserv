

#include "Socket/Socket.hpp"

#include <unistd.h>
#include <stdio.h>

int main()
{
    // Socket sock(AF_INET, SOCK_STREAM, 0, INADDR_ANY, 80, 10);
    // if (sock.err)
    //     return 1;
    // sock.launch();

    printf("%")
    if (0.1 + 0.2 == 0.3)
        write(1, "good", 4);
    else
        write(1, "bad", 3);
}